# CONTINUATION — read this first, every session

**Project**: RIFT: The Skyfell Engine — SNES ROM. Docs are law: [ROADMAP](./ROADMAP.md) (build order) · [GDD](./GDD.md) (design) · [ARCHITECTURE](./ARCHITECTURE.md) · [INVARIANTS](./INVARIANTS.md) · [DECISIONS](./DECISIONS.md) · **[JEREMY-INBOX](./JEREMY-INBOX.md) (decisions parked for Jeremy — read it, don't build past it)**.

## Resume prompt (paste into a fresh/compacted session)

> Continue RIFT: The Skyfell Engine, the SNES portal-gun game at `C:\Users\LCM\Github\skyfell`, building autonomously per Jeremy's standing authorization (commit per unit, push per phase/milestone, route his decisions to docs/JEREMY-INBOX.md and build around them). Read `docs/CONTINUATION.md` IN FULL first (env: the proven gate one-liner, MSYS2 make, MesenCE facts), plus the memory files `tcc816-mesen-traps` and `prophet-reuse-audit` (toolchain codegen traps — compound guards, ~1k-cycle calls, VMAIN $02 DMA, no function-scope statics/stack arrays, measure-with-scanline-probes-before-optimizing). Then JEREMY-INBOX (don't build past open items). State: Phases 0-2 COMPLETE, Phase 3 gravity chambers ~80% (Mode 7 + portal-driven gravity + test_gravity_cycle all green); gate baseline **11/11** cold-clean; playtest round 1 applied (D-013 two-button fire Y=blue/X=gold, stationary aim-lock, sentry shaft reachable). NEXT (in order): Gale Drone entity in the chamber, the crate puzzle chamber + test_chamber_puzzle, Jeremy's spin-feel gate, then Phase 3.5 demo dressing (Zone 1's 7 authored rooms, title card, audio spike) → MILESTONE A go/no-go (Jeremy's). Rails: goldens are HARVESTED from the ROM (temp test_zzz*.lua probes, deleted before commit; a change needs a DECISIONS entry); debug block append-only (next slot **+68**); dbg_lag==0 asserted in riding tests; every phase ends with a cold-clean `make test`, docs updated, commit+push, CI checked.

## Current state (2026-07-12, post-playtest-round-1)

- **Playtest round 1 applied (`abb148c`, pushed, gate 11/11)**: Jeremy's verdicts — controls reworked to **Y fires BLUE / X fires GOLD** (D-013; the fire+toggle scheme read as "can't shoot the second portal"), **aim-lock (R) freezes movement** (d-pad only aims; facing follows horizontal aims), license = **ALL RIGHTS RESERVED** (D-009 resolved), INV-ENG-004 exit-only revalidation **ratified**. The sentry shaft is now player-reachable (tower shortened to meta rows 18-27 + the row-18 girder extended onto its top; escape via pre-placed portal pair — noted in INBOX). test_fling's drop point moved below the new walkway (golden unchanged).

- **Phase 3 IN PROGRESS — PORTALS DRIVE GRAVITY, gate 11/11 cold-clean (`d6eab27`, pushed, CI pending)**: `test_gravity_cycle` runs the scripted 4-rotation loop through REAL portal transits TWICE, bit-identical. portal.c is world-switched (`portal_world`: room words vs chamber bytes, same 128 stride); chamber portals render as Mode 7 map-byte overlays (queue kinds M7SEQ/M7COL, $2118-only DMA, VMAIN $00/$02); transit ⇒ `chamber_set_gravity(portal_last_exit_or)` (gravity = -(exit normal) = identity in our encodings) with center re-anchor + raw-solid eject (the 17px floor-burial bug). **tcc816 trap added to the canon: `!a || !b` compound guards can miscompile** — portal_check's phantom transit fired with p_on {0,0}; split guards + in-loop rechecks (forensics probe pattern: write suspect state into idle dbg fields AT the suspect site).
- **Chamber portal render artifact FIXED (`8e65406`)**: vertical strips landed +1 stride — DMA to $2118 with VMAIN $02 strides +1 in MesenCE regardless of C shape (de-ternary refactors ruled out codegen; unresolved emu-vs-codegen, real-hardware check someday). Vertical portal strips now push six 1-byte M7SEQ entries; VRAM probe confirms exact placement + restore. **Toolchain law: never trust VMAIN $02 (+128) under DMA in this emulator.**
- **Phase 3 REMAINING**: (a) Gale Drone entity (radially symmetric sprite; entity.c is already world-aware via solid_at, but ent_update/render don't run in chamber mode yet — wire them + world→screen sprite transform for non-player sprites, 90° swaps per D-005, hide during the tween); (b) the puzzle chamber (2 reorientations + a crate → exit; needs chamber crate physics = gravity-frame crate_frame or reuse with gravity vector) + `test_chamber_puzzle`; (c) Phase 3's "spin feels good" gate is JEREMY's (INBOX); then Phase 3.5 demo dressing → MILESTONE A (Jeremy's go/no-go).
- **Phase 3 spike DONE earlier (`ae36d34`)**: chamber pipeline (`tools/art/mkchamber.py` — 8bpp world, 32×32-metatile ASCII arena `assets/maps/chamber01.txt` with brass on all 4 walls, palette 0-127 only, checksum; `tools/mklut.py` sin256), Mode 7 NMI shadows (`vq_set_m7*`), `src/game/chamber.c` gravity-frame physics (4-state gravity, tangent/along-G velocities, box swaps for horizontal G, world-space collision, eased rotation ROT_STEP, Wren pinned upright center), OBJ rebases to word $4000 in chamber / $2000 in rooms (`player_obj_base`). Warp room 1 = chamber; verb 9 = GRAV_SET(0-3). test_chamber cycles all 4 gravities with screenshots (landings: floor y=706, right wall x=706, ceiling y=288, left wall x=288).
- **Phase 3 REMAINING**: (a) portals in the chamber — portal.c hardcodes room01_map/att (hot-path direct reads); needs a map-source switch for chamber (cham_map is BYTES vs room words — likely a parallel chamber-side validator/overlay or a data-source indirection kept call-free); (b) gravity-from-portal-exit (chamber portal exit ⇒ chamber_set_gravity(exit orient opposite... gravity = -(exit outward normal) per GDD); (c) Gale Drone entity (radially symmetric); (d) the puzzle chamber layout (2 reorientations + a crate) + test_chamber_puzzle; (e) rename/extend test_chamber → test_gravity_cycle with bit-identical-state asserts per ROADMAP; (f) sprite world→screen transform for chamber entities (90° swaps + rotation during tween or hide).
- **Phase 2 COMPLETE** (the Rift Gun) — cold-clean gate **9/9**: boot, room, vblank, walk, jump, replay, **fling** (exact momentum conservation through the 16-entry LUT + portal render VRAM-assert), **portal_rules** (the shared INV-ENG-004 validator: non-brass/undersized/occupied fizzles, refire-moves, cell restore), **sentry** (the routed reflect-kill). Phases 0-1 shipped earlier (see ROADMAP + DECISIONS checkpoints).
- **Momentum is honest**: teleport = leading-edge plane crossing per axis, LUT transform, 6px/f cap, 1px/f eject, 8f cooldown; air friction is OFF (ground only) so flung |v| persists. INV-ENG-004 revalidates the EXIT portal only (INVARIANTS change log 2026-07-12).
- **Frame budget program (measured, not guessed)**: dbg_mainv (+62) + stage brackets (+64/+66) are permanent probes. tcc816 function calls ≈ 1k cycles — hot paths are call-free: `seams.asm` (MVN row moves, strided col gather) into zero-copy `vq_stage/vq_commit`; collision reads direct-indexed behind `portal_any` + portal bounding-box inline gates; call-free strip validator; live/crate entity lists; crates sleep; sentries idle beyond SENTRY_RANGE. Idle ~68-95 scanlines, event frames ~200, dbg_lag==0 asserted everywhere.
- **Golden numbers frozen from the ROM** (GDD-aligned tuning): walk60=154568 (gantry runway) · wall=4096 · apex=97888 (35.6px ≈ 2.2 metatiles) · land=107008. Max climbable ledge = 2 metatiles — check authored rooms against the apex.
- **Debug block**: `$7EFF00`, magic `0x51FE` (D-010); **next append at +68**; verbs 1 RESEED / 2 VQ_STRESS / 3 VQ_BUDGET / 4 POS_SET / 5 ENT_SPAWN / 6 PORTAL_SET / 7 WATCH / 8 ENT_CLEAR (tests/README.md is the contract). VRAM words `0x7C00+` = test scratch.
- **Git**: remote `github.com/Frostbite1536/skyfell`; Phase 1 pushed `9184be8` (CI green), Phase 2 = `a3c775a` + the gate commit. Overnight run 2026-07-11→12: Jeremy authorized full autonomy (commit per unit, push per phase, JEREMY-INBOX for his calls).
- **Open user decisions**: license (D-009), INV-ENG-004 refinement ratification, feel gates — all in JEREMY-INBOX.md.

## Environment facts (verified by running)

- **PVSnesLib 4.5.0** at `C:/Users/LCM/snesdev/pvsneslib` — `PVSNESLIB_HOME` forward-slash, exported inline.
- **The gate**: `C:\msys64\usr\bin\bash.exe -lc "export PVSNESLIB_HOME=C:/Users/LCM/snesdev/pvsneslib; make -C /c/Users/LCM/Github/skyfell test PYTHON=C:/Python314/python.exe"`
- **MesenCE 2.2.1** at `C:/Users/LCM/snesdev/mesence/Mesen.exe`; `emu.setInput(table, subport, PORT)` port is 3rd arg.
- **tcc816 laws learned by measurement here**: a function call ≈ 1k cycles — NEVER call per-tile/per-word in a loop that runs every frame (direct-index arrays, hoist wraps into runs, patch rare overlays after bulk copies); function-scope `static const` is forbidden (prophet's .data placement bug — file scope only); no signed right shift; u8 switch is a trap (if-else); ROM access via extern label indexing only.
- **Profiling pattern**: `vq_scanline()` + dbg_mainv/+64/+66 brackets; a temp `test_zzz*.lua` probe logs per-frame numbers to its artifact dir; lag attribution beats guessing (three wrong theories died to one measurement today).
- **pvsneslib gotchas**: `nmiSet()` overrides the default handler; `consoleUpdate()` DMAs to hardcoded $0800; CGRAM port is `*CGRAM_PALETTE`; force-blank time pollutes `lag_frame_counter` — loads/warps re-zero it (dbg_lag==0 means LIVE frames hold 60fps).
- Deeper toolchain lore: `../prophet/docs/CONTINUATION.md` ("Environment facts" + "Hardware/toolchain lessons").

## Next actions (in order) — Phase 3: Gravity Chambers (Mode 7)

1. **Spike the chamber RENDER first** (R2 mitigation, ROADMAP): a chamber room type with Mode 7 BG (128×128 map, 8bpp, VRAM $0000-$7FFF interleaved), dedicated VRAM/CGRAM layout behind a force-blank door transition; HUD-as-sprites (no BG3 in Mode 7); palette split INV-HW-006 (tiles 0-127, sprites 128-255). Generated chamber tileset via the mktiles pattern.
2. **Rotation math**: 256-entry sin/cos LUT (8.8) as generated .asm data; M7A-D + M7X/M7Y writes through the vblank queue (new vq entry kind or scroll-shadow-style latched registers — prefer latched shadows like scroll); world data never rotates (D-005).
3. **Gravity rule**: exit-portal normal = new "down"; transition freezes physics, eases θ over ~40 frames (tuning.h), snaps to 0/90/180/270; physics axes remap in the gravity frame; 4-state gravity (dbg_grav +16 already reserved).
4. **Gale Drone** (radially symmetric sprite, INV-HW-004) + one puzzle chamber (2 reorientations + a crate).
5. Tests: `test_gravity_cycle` (4 rotations → bit-identical state + screenshots per 90°), `test_chamber_puzzle`; replay stays green; watch the frame budget with the probes.
6. dbg appends start at +68. **The chamber is a separate room id / mode** — room.c's Mode 1 streaming doesn't apply inside (single-screen chambers, no scrolling — ROADMAP exclusion).

## Session gotchas learned (Phases 2-3 scars)

- (2026-07-12, Phase 3) **tcc816 compound-guard trap**: `if (!a || !b) return;` let a phantom portal transit through with both portals OFF (forensics: write suspect state into idle dbg fields AT the suspect site — caught p_on=={0,0} inside the success path). Split into separate ifs; recheck state inside loops.
- (2026-07-12, Phase 3) **VMAIN $02 (+128) DMA strides +1 in MesenCE** — vertical Mode 7 map strips must be per-cell pushes.
- (2026-07-12, Phase 3) Gravity-change box re-anchor: swap w/h around the CENTER and eject from RAW solids (portal holes count as walls for the eject — they close on recall).


- (2026-07-12) **Measure before optimizing**: the lag hunt burned three wrong theories (map_at cost, camera ease, transit revalidation) before the scanline probes attributed frames correctly. The probes are permanent now — use them FIRST.
- (2026-07-12) Portal teleports MUST check the leading edge between motion and solid-clamp per axis; center-based zones dead-end on thin openings (the sweep pins the body first, zeroing the entry velocity).
- (2026-07-12) A validator that consults `portal_cell()` (both portals) self-invalidates at transit time — check the OTHER portal explicitly.
- (2026-07-12) POS_SET warps take 60-150 emulated frames (force-blank redraw) — Lua ack timeouts ≥150.
- (2026-07-11) `make -C <dir>` is the reliable MSYS2 form; goldens are HARVESTED from the ROM via temp probes; identical goldens after a tuning edit = stale object (header-dep guard now in the Makefile).

## Handoff rules

End every session by updating: current phase/state, test counts (from the gate's own output, not memory), exact next actions, and anything learned that isn't in the docs. This file must stand alone — the next session reads it, not your chat history.
